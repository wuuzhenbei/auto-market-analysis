# 汽车市场数据分析项目 - 环境变量配置

# 数据库路径（可选，默认为 database/auto_market.db）
# DB_PATH=database/auto_market.db

# 采集配置（可选）
# REQUEST_TIMEOUT=30
# REQUEST_DELAY=2
# MAX_RETRIES=3

# 代理配置（可选，用于采集）
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

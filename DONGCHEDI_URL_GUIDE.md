# 懂车帝URL参数指南

## URL格式说明

根据用户提供的信息，懂车帝销量页面的URL格式为：

```
https://www.dongchedi.com/sales/sale-x-{参数1}-x-{参数2}-x-{参数3}-x-{参数4}-{品牌ID}
```

## 参数含义（待验证）

| 位置 | 参数 | 可能的值 | 说明 |
|------|------|----------|------|
| 参数1 | 车型类型 | 空=全部, 1=轿车, 2=SUV, 3=MPV, 4=新能源 | 需要验证 |
| 参数2 | 时间范围 | 202607=2026年7月, 500=近半年, 1000=近一年 | 需要验证 |
| 参数3 | 数据类型 | 零售量/批发量 | 需要验证 |
| 参数4 | 价格范围 | 具体数值或范围 | 需要验证 |
| 参数5 | 厂商属性 | 合资/自主/进口 | 需要验证 |
| 品牌ID | 品牌 | 483=问界, 2=奥迪, 等 | 需要验证 |

## 验证步骤

在使用这些参数之前，必须：

1. **手动访问URL**：在浏览器中打开URL，确认显示的数据是否正确
2. **核对参数含义**：通过对比不同URL的结果，确认每个参数的实际含义
3. **记录验证结果**：将验证后的参数含义记录下来

## 示例URL

用户提供的示例：
- `https://www.dongchedi.com/sales/sale-x-202606-x-x-x-x` - 2026年6月销量
- `https://www.dongchedi.com/sales/sale-x-500-x-x-x-x` - 近半年销量
- `https://www.dongchedi.com/sales/sale-x-1000-x-x-x-x` - 近一年销量
- `https://www.dongchedi.com/sales/sale-x-202607-x-x-x-483` - 2026年7月问界销量

## 注意事项

1. **不要猜测参数**：必须通过实际访问验证参数含义
2. **核对数据**：每次采集前都要确认URL返回的数据是否正确
3. **验证码问题**：频繁访问可能触发验证码，需要登录或降低频率
4. **反爬虫机制**：懂车帝有反爬虫机制，需要合理设置请求间隔

## 验证记录

在下方记录验证结果：

### 验证日期：____年____月____日

| URL | 验证结果 | 参数含义 |
|-----|----------|----------|
| | | |
| | | |

## 下一步行动

1. 手动访问用户提供的URL，验证参数含义
2. 记录验证结果
3. 根据验证结果编写采集脚本
4. 测试采集脚本，确保数据正确
